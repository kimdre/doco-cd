services:
  app:
    image: alpine:3.22
    command: ["sleep", "600"]
    # the hook runs after the container started, so a non-zero exit fails the
    # deployment AFTER the new commit is already stamped on the container
    post_start:
      - command: ["sh", "-c", "exit ${HOOK_EXIT:-0}"]
